export enum ServiceStepsEnum {
  upperCase = 'upperCase',
  lowerCase = 'lowerCase',
  removeSpaces = 'removeSpaces',
  gzip = 'gzip',
  ungzip = 'ungzip',
  encrypt = 'encrypt',
  decrypt = 'decrypt',
}
